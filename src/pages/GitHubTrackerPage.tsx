import React from 'react';
import { GithubTracker } from '../components/GithubTracker';

const GitHubTrackerPage: React.FC = () => {
  return (
    <div>
      <GithubTracker />
    </div>
  );
};

export default GitHubTrackerPage;
