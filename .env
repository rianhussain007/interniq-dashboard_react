# Frontend Configuration (for Vite)
# Prefix variables with VITE_ to expose them to the frontend code.

# The base URL of your backend API.
# For local development, this points to your FastAPI server.
# In production, this will be your Render deployment URL.
VITE_API_BASE_URL="http://127.0.0.1:8001"
